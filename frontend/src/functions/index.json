[
  {
    "type": "function",
    "function": {
      "name": "getDBSchema",
      "description": "Get the schema of the database being used.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "getUserData",
      "description": "Get the current user's data from the database.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "getDBData",
      "description": "Get the DB's data from tables. The tables are given and so are the columns. The columns appearing at the first index are used to establish relations to the table next to them.",
      "parameters": {
        "type": "object",
        "properties": {
          "tables": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The tables contributing to the final data. Not necessary to contain all tables."
          },
          "columns": {
            "type": "array",
            "items": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "description": "The columns to be used from the tables. First column is used for relation."
          }
        },
        "required": [
          "tables",
          "columns"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "navigateTo",
      "description": "Navigate to a different route/page in the application",
      "parameters": {
        "type": "object",
        "properties": {
          "route": {
            "type": "string",
            "description": "The route to navigate to (e.g., '/messages', '/', '/login', '/signup', '/ticket')"
          }
        },
        "required": ["route"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "fillInput",
      "description": "Fill an input field with a specified value",
      "parameters": {
        "type": "object",
        "properties": {
          "selector": {
            "type": "object",
            "properties": {
              "elementName": {
                "type": "string",
                "description": "The name attribute of the element"
              },
              "elementType": {
                "type": "string",
                "description": "The type of element (e.g., 'input', 'textarea', 'select')"
              },
              "elementId": {
                "type": "string",
                "description": "The ID of the element"
              },
              "elementClass": {
                "type": "string",
                "description": "The CSS class of the element"
              },
              "text": {
                "type": "string",
                "description": "Text content that the element contains or displays"
              }
            },
            "description": "Selector to identify the input element"
          },
          "value": {
            "type": "string",
            "description": "The value to fill into the input"
          }
        },
        "required": ["selector", "value"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "clickElement",
      "description": "Click on an interactive element like a button or link",
      "parameters": {
        "type": "object",
        "properties": {
          "selector": {
            "type": "object",
            "properties": {
              "elementName": {
                "type": "string",
                "description": "The name attribute of the element"
              },
              "elementType": {
                "type": "string",
                "description": "The type of element (e.g., 'button', 'a')"
              },
              "elementId": {
                "type": "string",
                "description": "The ID of the element"
              },
              "elementClass": {
                "type": "string",
                "description": "The CSS class of the element"
              },
              "text": {
                "type": "string",
                "description": "Text content that the element contains or displays"
              }
            },
            "description": "Selector to identify the element to click"
          }
        },
        "required": ["selector"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "submitForm",
      "description": "Submit a form",
      "parameters": {
        "type": "object",
        "properties": {
          "selector": {
            "type": "object",
            "properties": {
              "elementName": {
                "type": "string",
                "description": "The name attribute of the form"
              },
              "elementId": {
                "type": "string",
                "description": "The ID of the form"
              },
              "elementClass": {
                "type": "string",
                "description": "The CSS class of the form"
              },
              "elementType": {
                "type": "string",
                "description": "The type of element (should be 'form')"
              },
              "text": {
                "type": "string",
                "description": "Text content that the form contains"
              }
            },
            "description": "Selector to identify the form"
          }
        },
        "required": ["selector"]
      }
    }
  }
]